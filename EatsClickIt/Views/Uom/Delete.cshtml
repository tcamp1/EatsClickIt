@model EatsClickIt.Models.Uom

@{
    ViewBag.Title = "Uom - Delete";
}

<hr class="my-3" />
<h4>Are you sure you want to delete this?</h4>
<div>

    <hr class="my-3" />
    <h4>Uom - Delete</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.UofM)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.UofM)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Active)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Active)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Delete" class="btn btn-outline-primary btn-sm" />
            </div>
        </div>

    }
	<div>
		@Html.ActionLink("<- Back to List", "Index", null,  new { @class = "btn btn-outline-primary btn-sm" })
	</div>
</div>
